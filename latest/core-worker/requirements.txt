# Core Voice Worker Dependencies
# Python 3.10+ required

# ===================================================================
# LiveKit Agents Framework (Core)
# ===================================================================
livekit-agents[silero,turn-detector]~=1.2.0
livekit-api~=1.0.0

# ===================================================================
# Voice Pipeline Plugins
# ===================================================================
# OpenAI (LLM & TTS & STT)
livekit-plugins-openai~=0.10.0
openai>=1.50.0

# Noise Cancellation
livekit-plugins-noise-cancellation~=0.2.0

# Additional STT/TTS providers (optional, install as needed)
# livekit-plugins-deepgram~=0.6.0
# livekit-plugins-elevenlabs~=0.2.0
# livekit-plugins-cartesia~=0.2.0

# ===================================================================
# Configuration & Validation
# ===================================================================
pydantic~=2.9.0           # Data validation and settings management (updated for Python 3.13 compatibility)
python-dotenv~=1.0.0      # Environment variable loading

# ===================================================================
# HTTP Client & Async
# ===================================================================
aiohttp~=3.10.0           # Async HTTP client for API calls (updated for livekit-agents compatibility)
requests~=2.31.0          # Sync HTTP client (fallback)

# ===================================================================
# Development & Testing (optional)
# ===================================================================
# pytest~=7.4.0
# pytest-asyncio~=0.21.0
# black~=23.12.0
# mypy~=1.7.0
# ruff~=0.1.8

# ===================================================================
# Production Deployment
# ===================================================================
gunicorn~=21.2.0          # WSGI server (if needed for health checks)

# ===================================================================
# Notes:
# ===================================================================
# - Use ~= for compatible release version (allows patch updates)
# - Python 3.10+ required for modern async features
# - For production, pin exact versions for reproducibility
# - Install with: pip install -r requirements.txt
